{
  "name": "hardeepanand-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test tests/*.test.mjs",
    "verify": "node scripts/verify-site.mjs",
    "deploy": "npm run build && npm run test && npm run verify && wrangler pages deploy dist --project-name hardeepanand --branch main"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "marked": "^18.0.5"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
